# 安全说明

本仓库是 **SonettoHere 的第三方配套启动器**：它做的是「定位上游项目 → 拉起后端与前端 → 内嵌显示界面 → 优雅退出」，外加把上游的初始化 / 更新脚本搬进窗口里执行。下面把它**会执行什么、会联网做什么、会写什么**说清楚。

## 信任边界：它执行的是上游的脚本

- 它按你选定的上游项目目录，用命令行调用上游的 `main.py`、`setup_guide.py`、`upgrade.py`。
  也就是**这些脚本来自你 clone 的上游仓库**，不是本项目自带的。
- 本项目自带的两段包装器（`backend_wrapper.py`、`vite_run.mjs`）以**内嵌资源**随 exe 发布，运行时释放到 `%LOCALAPPDATA%\SonettoLauncher\wrappers\`，内容一致时不重写。
- 它**不修改上游任何文件**。唯一的例外是刻意的：`setup_guide.py` 会用模板覆盖 `config/personas/` 下的个性文件，所以「初始化环境」与「检查更新」之前都会先自动备份 `config/` 关键文件（保留最近 10 份）。

**因此：你信任的是你所 clone 的那个上游仓库。** 若上游被改动或被替换成别的来源，启动器会照常执行它的脚本——请自行确认上游来源。

## 联网行为

- **启动器自身的 HTTP 只打本机**：`Native/Net.cs` 里的 HttpClient 用于对上游那两个本地服务端口（默认后端 `:8000`、前端 `:5173`）做探活，不向任何外部地址发请求。
- **联网发生在「检查更新」**：它执行上游的 `upgrade.py`，其中包含 `git pull`——那是上游的行为，请求发往你配置的上游远端。
- 启动器**不收集、不上传任何数据**，没有遥测。

## 它会写什么

只写自己的数据目录 `%LOCALAPPDATA%\SonettoLauncher\`：日志（按会话分文件，保留 7 天）、配置备份（保留最近 10 份）、WebView2 用户数据（登录态、localStorage）。此外不落任何文件。

## 报告问题

- **安全相关的发现**：用本仓库的 **Security → Report a vulnerability** 开私密 advisory，不要开公开 issue。
- **一般缺陷**：开 issue 即可。请带上启动器版本（窗口标题里那个，或 Release 标签）、Windows 版本、复现步骤，以及 `%LOCALAPPDATA%\SonettoLauncher\logs\` 下对应的 `launcher-*.log`（关闭流程的问题通常在那条停止记录里就能看出是优雅退出还是被强杀）。

## 不在范围内

上游 SonettoHere 本体及其脚本的行为、.NET 运行时与 WebView2 运行时本身、以及你自己机器上的端口暴露（后端默认监听本机端口，请勿在不可信网络里对外放开）。
